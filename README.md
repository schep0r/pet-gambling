`./vendor/bin/sail up` # Start the docker container

`./vendor/bin/sail npm run dev` # Compile the asse

`./vendor/bin/sail artisan migrate` # Run the migrations

`./vendor/bin/sail artisan db:seed` # Run the seeders
